#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
	long long int n, t;
    while(cin >> n && n) {
    	t = sqrt(n);
    	if(t*t == n) cout << "yes" << endl;
    	else cout << "no" << endl;
    }
  return 0;
}	
